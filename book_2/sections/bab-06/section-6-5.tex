\section{Kesimpulan Analisis Semantik}

Analisis semantik menjembatani antara tugas parser (struktur) dan code generator (instruksi). Keberhasilan fase ini menjamin bahwa program yang dikompilasi memiliki makna yang konsisten dan mematuhi spesifikasi bahasa.

\begin{figure}[!htbp]
    \centering
    \adjustbox{max width=0.8\textwidth,center}{%
    \begin{tikzpicture}[
        node/.style={rectangle, draw=blue!50, fill=blue!10, font=\tiny, align=center},
        arrow/.style={->, >=stealth, thick}
    ]
    \node[node] (syntax) {Syntax Analysis (Parser)};
    \node[node, below=1cm of syntax] (semantic) {Semantic Analysis};
    \node[node, below=1cm of semantic] (ir) {Intermediate Representation};
    \draw[arrow] (syntax) -- (semantic);
    \draw[arrow] (semantic) -- (ir);
    \end{tikzpicture}%
    }
    \caption{Aliran informasi melalui fase semantik}
\end{figure}
